// import { parseAndCreateWords } from './api/SearchApi';
import App from './App.svelte';

// parseAndCreateWords();

const app = new App({
	target: document.body,
	props: {},
});

export default app;
